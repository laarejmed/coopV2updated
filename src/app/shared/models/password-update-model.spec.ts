import { PasswordUpdateModel } from './password-update-model';

describe('PasswordUpdateModel', () => {
  it('should create an instance', () => {
    expect(new PasswordUpdateModel()).toBeTruthy();
  });
});
