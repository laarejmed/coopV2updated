import { UserBankItemModel } from './user-bank-item-model';

describe('UserBankItemModel', () => {
  it('should create an instance', () => {
    expect(new UserBankItemModel()).toBeTruthy();
  });
});
